Test Questions — Study Assistant
Ask these inside your Claude Project to check it is grounded correctly.

1. What is the difference between supervised and unsupervised learning?
   Expect: A clear contrast citing Chapter 1.

2. What is backpropagation?
   Expect: Step 3 of training — computing each weight's contribution to the error (Chapter 2).

3. How is precision different from recall?
   Expect: Precision = TP/(TP+FP); Recall = TP/(TP+FN), with plain-English meaning (Chapter 3).

4. What does ReLU do?
   Expect: Outputs max(0, x); adds non-linearity (Chapter 2).

5. Explain how transformers and attention work.
   Expect: "That topic isn't covered in the current chapter notes..." (not in the notes).
